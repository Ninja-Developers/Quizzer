const initialState = {
    isLoggedIn: false,
    username: '',
    token: ''
}

export default (state = initialState, action) => {
    switch(action.types){
        default:
            return state
    }
}